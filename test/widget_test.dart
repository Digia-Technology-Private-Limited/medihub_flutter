import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — the default counter test doesn't apply to MediHub.
    expect(1 + 1, equals(2));
  });
}
